# This is a dummy so that we can specify english and get the base bundle instead of the default bundle (which may be another language).
#
# IOW, if this file did not exist, ResourceBundle.getBundle(baseName, Locale.ENGLISH) 
# would return the "default" Locale instead of the base locale
